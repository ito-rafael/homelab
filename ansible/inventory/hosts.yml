lbic:
  vars:
    ansible_user: ansible
  children:

    lbic_server:
      hosts:

        andira:
          ansible_host: andira
          ansible_user: root

        proxmox:
          ansible_host: andira
          ansible_user: root
