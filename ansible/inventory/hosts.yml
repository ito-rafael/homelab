lbic:
  vars:
    ansible_user: ansible
  children:

    lbic_experiment:
      hosts:
        paubrasil:    # xxx.xx.xx.179

    lbic_server:
      hosts:

        andira:
          ansible_host: andira
          ansible_user: root

        proxmox:
          ansible_host: andira
          ansible_user: root

        vyos:
          ansible_host: 10.10.10.1
          ansible_user: vyos
          ansible_connection: network_cli
          ansible_network_os: vyos.vyos.vyos
          ansible_ssh_common_args: '-J root@andira'

        headscale:
          ansible_host: 10.10.20.2
          ansible_user: root
          ansible_ssh_common_args: '-J root@andira'
